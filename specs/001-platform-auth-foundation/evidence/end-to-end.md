# End-to-end evidence

Playwright passed 12/12 live journeys in 50.5 seconds across `local` and `unrelated-origins` projects against disposable PostgreSQL and SMTP capture. Covered outcomes include registration/verification, verified sign-in and own profile, trusted-route denial, reload restoration, logout/device isolation, regular-user administrator denial, safe credential errors, and zero browser-storage persistence.
