# Clean-start evidence

The dependency lockfile, `.env.example` files, fail-fast validators, Prisma generation/migration commands, bootstrap command, and development scripts are present and documented. Missing/malformed server values, wildcard CORS, weak secrets, and public client classification have passing automated tests.

The disposable acceptance environment applied the canonical migration, initialized the administrator, started Express under Node.js 24, and brought Next.js to ready state. With downloaded prerequisites and external services available, both application listeners became healthy in under 15 seconds. Missing/malformed configuration tests passed before the live run.
